# 20260915 当日开发计划(22-公司 cron 自补)

> **生成方式**:22-公司 cron 03:30 自补(因 T4 昨日未写 plan,沿用 09-09/10/11/12/14 自补模式)
> **目标**:跨行业 schema v0.1.5 — 跨行业 5 公司对比矩阵
> **预计时长**:30-40 分钟(单一 .md 追加 + 同一 .py 加 §G 段 + 自检)

## 1. 今日目标

把 v0.1.4 5 公司跨行业 dryrun(单家公司校验)的成果,升级为"跨行业 5 公司横向对比矩阵",让张勇拉群时可直接看 5 家公司在 8-10 个关键维度上的可比表,而不是要看 5 个独立的 JSON 块。

## 2. 具体产出(3 项)

### 2.1 `Inspiration/跨行业/01-跨行业公司画像字段schema设计稿.md` 追加

- **§15 跨行业 5 公司对比矩阵**(本节增量)
  - §15.1 8 指标横向对比表(5 公司 × 8 指标:护城河分、营收 2024、毛利率 2024、ROE 2024、负债率 2024、自由流通、扩展位、行业分类)
  - §15.2 跨行业护城河评分分布(5 公司 moat_score 排序 + 区间标注)
  - §15.3 营收 5 年趋势(5 公司 × 2020-2024 共 25 数据点对比)
  - §15.4 行业扩展位填法对比(3 行业 × 各自代表公司,展示 bio / macro / profitability 扩展位的字段填法差异)
  - §15.5 §G 跑批输出(机器可读矩阵 dryrun 实际跑通)

- **§16 v0.1.5 vs v0.1.4 增量**(本节增量)
  - 新增内容清单
  - 保留内容清单
  - 文件名不变(沿用 v0.1.3 后缀)

- **§17 v0.1.5 不做**(本节边界)
  - 沿用 v0.1.4 §14.4 不做原则,补充本节新增边界

### 2.2 `Inspiration/跨行业/02-跨行业公司画像schema-Pydantic雏形-v0.1.3.py` 追加

- **新增 `build_cross_industry_matrix()` 函数**:遍历 ALL_EXAMPLES → CrossIndustryCompanyProfile → 抽取 8 指标 → 返回 dict
- **新增 §G 跨行业对比矩阵 dryrun 段**(在 §F 之后)
- **更新 docstring**:v0.1.4 → v0.1.5
- **保留 §A-§F 6 段自检全部不变**

### 2.3 `项目开发计划.md` §5 #6 更新

- 新增 2026-09-15 推进段落:跨行业 schema v0.1.5 自补(对比矩阵)
- 仍然不勾选 §5 #6(共识未发生)
- 仍然不勾选 #6 子项(0/1 不变)

## 3. 不做(本 cron 边界)

- 不勾选 §5 #6 checkbox(共识仍未发生)
- 不改 §11 Pydantic schema 主体 / 32 字段 / validator / §5 C1-C8 约束
- 不触发任何 connector / 真实数据拉取
- 不拆分 .py 文件
- 不写 README 章节
- 不替 3 行业定字段语义
- 不覆盖 §5 C1-C7 违规检测(顺延到 v0.2 共识会议后)
- 不输出 JSON Schema 文件版本固化(顺延到 v0.2 共识)

## 4. 验证标准

- [ ] §G 跑通,5 公司矩阵输出齐全(8 指标 × 5 公司 = 40 数据点)
- [ ] §A-§F 6 段自检全部保持通过(不破坏现有功能)
- [ ] git diff 干净(只追加,不动既有内容)
- [ ] Gitee push 成功(主仓库)
- [ ] GitHub push 尝试(允许失败,30s 超时)

## 5. 完成后

- 删除本 plan 文件
- 报告 commit hash + Gitee status + GitHub status